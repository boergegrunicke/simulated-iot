from setuptools import setup, find_packages

setup(
    name="simlauted-iot",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Børge Grunicke",
    description="Tiny simluator that behaves like an iot gateway",
    python_requires=">=3.12"
)